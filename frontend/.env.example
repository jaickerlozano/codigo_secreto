# Client-visible values only. VITE_ variables are bundled into the browser and must never contain secrets.
VITE_API_URL=http://localhost:8000
VITE_API_TIMEOUT_MS=10000
VITE_SUPPORT_PHONE=56912345678

# Production format only. The deployment owner supplies the approved hostname outside this repository.
# VITE_API_URL=https://<assigned-api-hostname>
# VITE_API_TIMEOUT_MS=10000
