# Development-only PostgreSQL values. Copy this file to postgres.env and replace
# the password locally. Do not use these values for production.
POSTGRES_DB=codigo_secreto
POSTGRES_USER=codigo_secreto
POSTGRES_PASSWORD=replace-with-a-local-alphanumeric-password
